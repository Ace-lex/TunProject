name: test

on:
  push:
    branches: [ "main" ]


jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v2
  test:
    needs: build
    runs on: ubuntu-latest
    steps: 
      - run: cd Test && ./test.sh
    
